spring:
  application:
    name: bigdata-eurekaserver
eureka:
  instance:
    hostname: localhost
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8762/eureka/